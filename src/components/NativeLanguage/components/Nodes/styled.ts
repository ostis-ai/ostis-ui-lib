import styled from 'styled-components';

import { NativeLink } from '../NativeLink';

export const StyledNativeLink = styled(NativeLink)`
  font-weight: 500;
  line-height: 26px;
  font-size: 22px;
`;

export const KeywordLinkWrapper = styled.div`
  display: flex;
`;

export const LeftSide = styled.span`
  width: 20px;

  color: #2a6496;

  flex-shrink: 0;
`;
